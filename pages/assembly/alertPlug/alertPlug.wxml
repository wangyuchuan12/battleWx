<template name="alertPlug">
  <view class="alertPlug" wx:if="{{isShow}}">
    <view class="alertPlugContent">{{content}}</view>
    <button class="alertPlugButton" bindtap='alertPlugConfirmClick'>确定</button>
  </view>
</template>
